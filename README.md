# Face Value

A limit order book, a market-making simulator, and a live recorder of real prediction-market
order books — built to measure what adverse selection costs a market maker around a
scheduled information release. See `docs/PROJECT.md` for the full framing and roadmap.

Run the tests: `python -m pytest -v`
Start the recorder: `python data/record.py`
